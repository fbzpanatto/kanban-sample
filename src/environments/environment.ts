// src/environments/environment.ts (desenvolvimento)
export const environment = {
  production: false,
  useMockData: true, // true aqui = usa o KanbanMockService por padrão em dev
  apiUrl: 'http://localhost:5000/api'
};